apply plugin: 'eclipse'
apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    groovy group: 'org.codehaus.groovy', name: 'groovy', version: '2.0.6'
    groovy group: 'org.codehaus.gpars', name: 'gpars', version: '1.0.0'
            
}
